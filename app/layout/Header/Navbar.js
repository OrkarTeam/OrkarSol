import clsx from 'clsx';

const Navbar = () => {
  return ( 
    <div className={clsx(['scene__one', 'scene__two', 'h-32', 'w-screen'])}>this is the navbar component</div>
  );
}

export default Navbar;